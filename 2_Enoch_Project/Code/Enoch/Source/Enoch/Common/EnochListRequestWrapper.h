#pragma once

#include <vector>

#include "Enoch/EnochGameInstance.h"
#include "Enoch/Common/EnochFreeLancerData.h"

/**
 * 유저 정보 요청용 함수 모음 클래스	<br>
 * 추후 함수 내부에서 객체를 다룰 가능성을 염두해 모두 non-static 함수로 구현
 */
class EnochListRequestWrapper
{
public:
	EnochListRequestWrapper();
	~EnochListRequestWrapper();


	
};
